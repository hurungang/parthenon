/**
 * Re-export the SetupWizard from the features module so that AppRouter.tsx
 * import paths remain unchanged while the implementation lives in features/setup/.
 */
export { SetupWizard } from '../../features/setup/SetupWizard'
